#!/usr/bin/env bash

WALLPAPERS=($HOME/.config/hypr/wallpapers/*.jpg)
RANDOM_WP=${WALLPAPERS[RANDOM % ${#WALLPAPERS[@]}]}

# Set wallpaper with fade animation
swww img "$RANDOM_WP" --transition-type fade --transition-step 1 --transition-fps 60

